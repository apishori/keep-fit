import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const Cals = () =>{
	return(
		<View style={{flex:1}}>
			<Text>Cals Screen</Text>
		</View>
	)
}

export default Cals